import 'package:flutter/material.dart';

class CurrentDetailPage extends StatelessWidget {
  const CurrentDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
